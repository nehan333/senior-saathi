stages:
  - lint
  - audit

lint:
  stage: lint
  image: python:3.12
  script:
    - pip install ruff
    - ruff check .

audit:
  stage: audit
  image: python:3.12
  script:
    - python -m pip install --upgrade pip
    - pip install pip-audit
    - pip-audit
